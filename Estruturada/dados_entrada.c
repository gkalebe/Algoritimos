#include <stdio.h>
#include <stdlib.h>


/*faça um programa que receba como dados de entrada 3 números inteiros utilizando apenas 2 variáveis */

int main(){
    int controle = 1, valor;
        while(controle !=0){
            printf("Digite um numero");
            scanf("%d", &valor);

        }
    print("Digite 1 para continuar \n ou 0 para sair");
        scanf("%d", &controle);
}